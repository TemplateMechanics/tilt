{%- if values.deploymentType == 'helm-release' and values.helmRepoUrl %}
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: ${{ values.helmRepo | default(values.name) }}
  namespace: flux-system
spec:
  interval: 1h
  url: ${{ values.helmRepoUrl }}
{%- endif %}
