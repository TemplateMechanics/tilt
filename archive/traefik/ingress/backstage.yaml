apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backstage
  namespace: backstage
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`backstage.localhost`)
      kind: Rule
      services:
        - name: backstage
          port: 7000
  tls: {}
