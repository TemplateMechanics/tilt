---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - providers.yaml
  # ProviderConfigs must be applied after providers install their CRDs
  # Apply separately: kubectl apply -k helm/crossplane/providers/configs/

