apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-config
  namespace: backstage
data:
  app-config.yaml: |
    app:
      title: Dev Portal
      baseUrl: https://backstage.localhost

    organization:
      name: SysTechs

    backend:
      baseUrl: https://backstage.localhost
      listen:
        port: 7000
      csp:
        connect-src: ["'self'", 'http:', 'https:']
      cors:
        origin: https://backstage.localhost
        methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
        credentials: true
      database:
        client: pg
        connection:
          host: ${POSTGRES_HOST}
          port: ${POSTGRES_PORT}
          user: ${POSTGRES_USER}
          password: ${POSTGRES_PASSWORD}
          database: backstage_db

    integrations:
      github:
        - host: github.com
          token: ${GITHUB_TOKEN}

    techdocs:
      builder: 'local'
      generator:
        runIn: 'local'
      publisher:
        type: 'local'

    auth:
      providers:
        guest:
          dangerouslyAllowOutsideDevelopment: true

    catalog:
      import:
        entityFilename: catalog-info.yaml
        pullRequestBranchName: backstage-integration
      rules:
        - allow: [Component, System, Domain, Group, API, Resource, Location, Template]
      locations:
        # Load catalog from locally mounted ConfigMap
        - type: file
          target: /usr/src/app/catalog/all.yaml
          rules:
            - allow: [Component, System, Domain, Group, API, Resource, Location, Template]

    # Kubernetes plugin config - connects to local cluster
    kubernetes:
      serviceLocatorMethod:
        type: 'multiTenant'
      clusterLocatorMethods:
        - type: 'config'
          clusters:
            - url: https://kubernetes.default.svc
              name: local-dev
              authProvider: 'serviceAccount'
              skipTLSVerify: true
              skipMetricsLookup: true

    # Tilt integration (custom plugin endpoint)
    tilt:
      baseUrl: http://host.docker.internal:10350
