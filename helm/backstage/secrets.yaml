apiVersion: v1
kind: Secret
metadata:
  name: backstage-secrets
  namespace: backstage
type: Opaque
stringData:
  POSTGRES_HOST: "bstage-db"
  POSTGRES_PORT: "5432"
  POSTGRES_USER: "bstage_user"
  POSTGRES_PASSWORD: "bstage-dev-password"
  # Provide GITHUB_TOKEN via a secure mechanism (ExternalSecrets, 1Password, or local-only secret creation)
  GITHUB_TOKEN: ""
