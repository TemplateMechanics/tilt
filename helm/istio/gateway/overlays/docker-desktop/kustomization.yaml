---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Docker Desktop provides a LoadBalancer that binds host localhost:80/443,
# which is the Istio default — no patch needed.
resources:
  - ../../base
