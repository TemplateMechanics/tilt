---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Requires: cert-manager CRDs + local-intermediate-ca ClusterIssuer Ready,
# and the istio GatewayClass registered.
resources:
  - certificate.yaml
  - gateway.yaml
