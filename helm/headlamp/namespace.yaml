---
apiVersion: v1
kind: Namespace
metadata:
  name: headlamp
  labels:
    # Deliberately NOT ambient. Headlamp talks to the kube-apiserver, which is
    # outside the mesh, and this namespace serves a UI rather than a workload
    # worth observing in Kiali.
    istio.io/dataplane-mode: none
