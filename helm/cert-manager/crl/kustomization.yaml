---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The CRL bytes are generated from the live CA keys by
# scripts/ensure-local-crls.sh and stored in ConfigMap/local-ca-crls. They are
# deliberately not committed to git.
resources:
  - server.yaml
  - route.yaml
