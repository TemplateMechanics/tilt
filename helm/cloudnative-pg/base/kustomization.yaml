---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Operator only. Cluster resources live in ../clusters, which needs the CRDs
# this release installs.
resources:
  - namespace.yaml
  - helm-repo.yaml
  - helm-release.yaml
