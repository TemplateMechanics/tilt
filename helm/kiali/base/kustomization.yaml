---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Requires istio-system to exist (created by helm/istio) and the Gateway to be
# programmed before the route resolves.
resources:
  - helm-repo.yaml
  - helm-release.yaml
  - httproute.yaml
